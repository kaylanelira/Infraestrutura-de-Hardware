addi x11, x0, 13

gets:
	#slli x12, x13, 3
	lw x10, 1025(x0)
	#add x12, x12, x10
	#lw x9, 0(x12)
	# lw x10, 1025(x0)
	beq x10, x11, print
	addi sp, sp, -8
	sw x10, 0(sp)
	#addi x13, x13, 1
	beq x0, x0, gets
	#beq x0, x0, print

print:
	lw x10, 1024(x0)
	addi sp, sp, 8
	bne x10, x11, print
halt
# i = x13
# key[i] = x10
